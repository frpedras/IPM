<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iscte.ipm.ipm" >

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ChangeName"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
        android:name=".MenuPrincipal"
        android:label="Menu"
        >
        </activity>

        <activity
            android:name=".ConfigurationsP"
            android:label="Configurations Patient"
            >
        </activity>

        <activity
            android:name=".ConfigurationsA"
            android:label="Configurations Agent"
            >
        </activity>

        <activity
            android:name=".ConfigurationsPsi"
            android:label="Configurations Psychologist"
            >
        </activity>

        <activity
            android:name=".SelectFood"
            android:label="Select Food"
            >
        </activity>

        <activity
            android:name=".CreatingAlert"
            android:label="Creating Alert"
            >
        </activity>

        <activity
            android:name=".Alerts"
            android:label="Alerts"
            >
        </activity>

        <activity
            android:name=".ParamFood"
            android:label="Configurations Food"
            >
        </activity>

        <activity
            android:name=".ParamLevel"
            android:label="Configurations Level"
            >
        </activity>

        <activity
            android:name=".CreateFood"
            android:label="Create Food"
            >
        </activity>

        <activity
            android:name=".Game"
            android:label="Game"
            >
        </activity>
    </application>

</manifest>
